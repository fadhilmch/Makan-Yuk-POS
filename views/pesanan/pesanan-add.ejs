<!doctype html>

<head>
  <meta charset="utf-8">
  <title>Makan Yuk</title>

  <link rel="stylesheet" href="/css/style.css" type="text/css"></head>
 <body>
    <h1>Tambah Pesanan</h2>

    <form action="/meja/:id/pesan" method="post">
        Menu yang dipesan:</br>
        <table>
            <tr>
                <th>Nama Menu</th>
                <th>Jumlah</th>
            </tr>
            <tr>
                <td>
                    <select name='MenuId'>
                         <% for(let i = 0; i < menus.length; i++){%>
                             <option value = "<%=menus[i].id%>"><%=menus[i].name%></option>
                         <%}%>
                    </select><br>
                </td>
                <td>
                    <input type='text' name='quantity'>
                </td>
            </tr>
        </table>
    </form>


 </body>
